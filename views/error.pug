extends layout

block body
  main.auth
    .auth-card.center
      p.error-code= status
      h1= title
      p.muted= message
      if stack
        details.stack-trace
          summary Stack trace
          pre= stack
      a.btn.btn-primary(href="/") Back to my files
